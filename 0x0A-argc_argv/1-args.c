#include <stdio.h>

/**
 * main - prints the number of arguments passed into it
 * @argc: the number of arguments passed
 *
 * Return: Always 0
 */
int main(int argc)
{
printf("%d\n", argc - 1);

return (0);
}
